function debug (message, message2) {
  // TODO: remove password values
  // TODO: Use args parameter in case more than 2 values passed
  if (message2) {
    // console.log(message, message2)
  } else {
    // console.log(message)
  }
}

module.exports = {
  debug
}
